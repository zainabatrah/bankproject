VITE_SOC_API_URL=http://localhost:3000/soc
# Set this for a separately hosted dashboard. In a same-origin deployment,
# the dashboard also reads bankshield.auth.session.v1 from browser storage.
VITE_SOC_ACCESS_TOKEN=
